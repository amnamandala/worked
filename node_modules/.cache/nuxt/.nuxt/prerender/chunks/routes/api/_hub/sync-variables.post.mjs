import { eventHandler, readValidatedBody, getHeader } from 'file://C:/Users/Eman/Documents/GitHub/worked/node_modules/h3/dist/index.mjs';
import { z } from 'file://C:/Users/Eman/Documents/GitHub/worked/node_modules/zod/index.js';
import { r as requireNuxtHubAuthorization } from '../../../_/auth.mjs';
import 'file://C:/Users/Eman/Documents/GitHub/worked/node_modules/ofetch/dist/node.mjs';

const syncVariables_post = eventHandler(async (event) => {
  await requireNuxtHubAuthorization(event);
  const { env, keys } = await readValidatedBody(event, z.object({
    env: z.string(),
    keys: z.array(z.string())
  }).parse);
  const variables = keys.map((key) => ({ key, value: process.env[key] }));
  await $fetch(`/api/projects/${process.env.NUXT_HUB_PROJECT_KEY}/variables/sync-from-deployment`, {
    baseURL: process.env.NUXT_HUB_URL || "https://admin.hub.nuxt.com",
    method: "POST",
    body: {
      env: process.env.NUXT_HUB_ENV || env,
      variables
    },
    headers: new Headers({
      authorization: getHeader(event, "authorization") || ""
    })
  });
  return {
    success: true
  };
});

export { syncVariables_post as default };
//# sourceMappingURL=sync-variables.post.mjs.map
